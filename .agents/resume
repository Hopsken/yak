#!/usr/bin/env bash
set -euo pipefail

# Dependencies are retained. Restarting the disposable network creates new
# accounts; merge .env.test-network and restart Yak as documented.
echo 'Orb ready; see docs/development-network.md for local PDS startup'
